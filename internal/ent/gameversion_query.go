// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/open-boardgame-stats/backend/internal/ent/game"
	"github.com/open-boardgame-stats/backend/internal/ent/gameversion"
	"github.com/open-boardgame-stats/backend/internal/ent/match"
	"github.com/open-boardgame-stats/backend/internal/ent/predicate"
	"github.com/open-boardgame-stats/backend/internal/ent/schema/guidgql"
	"github.com/open-boardgame-stats/backend/internal/ent/statdescription"
)

// GameVersionQuery is the builder for querying GameVersion entities.
type GameVersionQuery struct {
	config
	ctx                       *QueryContext
	order                     []gameversion.OrderOption
	inters                    []Interceptor
	predicates                []predicate.GameVersion
	withGame                  *GameQuery
	withStatDescriptions      *StatDescriptionQuery
	withMatches               *MatchQuery
	withFKs                   bool
	modifiers                 []func(*sql.Selector)
	loadTotal                 []func(context.Context, []*GameVersion) error
	withNamedStatDescriptions map[string]*StatDescriptionQuery
	withNamedMatches          map[string]*MatchQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the GameVersionQuery builder.
func (gvq *GameVersionQuery) Where(ps ...predicate.GameVersion) *GameVersionQuery {
	gvq.predicates = append(gvq.predicates, ps...)
	return gvq
}

// Limit the number of records to be returned by this query.
func (gvq *GameVersionQuery) Limit(limit int) *GameVersionQuery {
	gvq.ctx.Limit = &limit
	return gvq
}

// Offset to start from.
func (gvq *GameVersionQuery) Offset(offset int) *GameVersionQuery {
	gvq.ctx.Offset = &offset
	return gvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (gvq *GameVersionQuery) Unique(unique bool) *GameVersionQuery {
	gvq.ctx.Unique = &unique
	return gvq
}

// Order specifies how the records should be ordered.
func (gvq *GameVersionQuery) Order(o ...gameversion.OrderOption) *GameVersionQuery {
	gvq.order = append(gvq.order, o...)
	return gvq
}

// QueryGame chains the current query on the "game" edge.
func (gvq *GameVersionQuery) QueryGame() *GameQuery {
	query := (&GameClient{config: gvq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := gvq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := gvq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(gameversion.Table, gameversion.FieldID, selector),
			sqlgraph.To(game.Table, game.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, gameversion.GameTable, gameversion.GameColumn),
		)
		fromU = sqlgraph.SetNeighbors(gvq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryStatDescriptions chains the current query on the "stat_descriptions" edge.
func (gvq *GameVersionQuery) QueryStatDescriptions() *StatDescriptionQuery {
	query := (&StatDescriptionClient{config: gvq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := gvq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := gvq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(gameversion.Table, gameversion.FieldID, selector),
			sqlgraph.To(statdescription.Table, statdescription.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, true, gameversion.StatDescriptionsTable, gameversion.StatDescriptionsPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(gvq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryMatches chains the current query on the "matches" edge.
func (gvq *GameVersionQuery) QueryMatches() *MatchQuery {
	query := (&MatchClient{config: gvq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := gvq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := gvq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(gameversion.Table, gameversion.FieldID, selector),
			sqlgraph.To(match.Table, match.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, gameversion.MatchesTable, gameversion.MatchesColumn),
		)
		fromU = sqlgraph.SetNeighbors(gvq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first GameVersion entity from the query.
// Returns a *NotFoundError when no GameVersion was found.
func (gvq *GameVersionQuery) First(ctx context.Context) (*GameVersion, error) {
	nodes, err := gvq.Limit(1).All(setContextOp(ctx, gvq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{gameversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (gvq *GameVersionQuery) FirstX(ctx context.Context) *GameVersion {
	node, err := gvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first GameVersion ID from the query.
// Returns a *NotFoundError when no GameVersion ID was found.
func (gvq *GameVersionQuery) FirstID(ctx context.Context) (id guidgql.GUID, err error) {
	var ids []guidgql.GUID
	if ids, err = gvq.Limit(1).IDs(setContextOp(ctx, gvq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{gameversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (gvq *GameVersionQuery) FirstIDX(ctx context.Context) guidgql.GUID {
	id, err := gvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single GameVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one GameVersion entity is found.
// Returns a *NotFoundError when no GameVersion entities are found.
func (gvq *GameVersionQuery) Only(ctx context.Context) (*GameVersion, error) {
	nodes, err := gvq.Limit(2).All(setContextOp(ctx, gvq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{gameversion.Label}
	default:
		return nil, &NotSingularError{gameversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (gvq *GameVersionQuery) OnlyX(ctx context.Context) *GameVersion {
	node, err := gvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only GameVersion ID in the query.
// Returns a *NotSingularError when more than one GameVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (gvq *GameVersionQuery) OnlyID(ctx context.Context) (id guidgql.GUID, err error) {
	var ids []guidgql.GUID
	if ids, err = gvq.Limit(2).IDs(setContextOp(ctx, gvq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{gameversion.Label}
	default:
		err = &NotSingularError{gameversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (gvq *GameVersionQuery) OnlyIDX(ctx context.Context) guidgql.GUID {
	id, err := gvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of GameVersions.
func (gvq *GameVersionQuery) All(ctx context.Context) ([]*GameVersion, error) {
	ctx = setContextOp(ctx, gvq.ctx, "All")
	if err := gvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*GameVersion, *GameVersionQuery]()
	return withInterceptors[[]*GameVersion](ctx, gvq, qr, gvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (gvq *GameVersionQuery) AllX(ctx context.Context) []*GameVersion {
	nodes, err := gvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of GameVersion IDs.
func (gvq *GameVersionQuery) IDs(ctx context.Context) (ids []guidgql.GUID, err error) {
	if gvq.ctx.Unique == nil && gvq.path != nil {
		gvq.Unique(true)
	}
	ctx = setContextOp(ctx, gvq.ctx, "IDs")
	if err = gvq.Select(gameversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (gvq *GameVersionQuery) IDsX(ctx context.Context) []guidgql.GUID {
	ids, err := gvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (gvq *GameVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, gvq.ctx, "Count")
	if err := gvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, gvq, querierCount[*GameVersionQuery](), gvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (gvq *GameVersionQuery) CountX(ctx context.Context) int {
	count, err := gvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (gvq *GameVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, gvq.ctx, "Exist")
	switch _, err := gvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (gvq *GameVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := gvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the GameVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (gvq *GameVersionQuery) Clone() *GameVersionQuery {
	if gvq == nil {
		return nil
	}
	return &GameVersionQuery{
		config:               gvq.config,
		ctx:                  gvq.ctx.Clone(),
		order:                append([]gameversion.OrderOption{}, gvq.order...),
		inters:               append([]Interceptor{}, gvq.inters...),
		predicates:           append([]predicate.GameVersion{}, gvq.predicates...),
		withGame:             gvq.withGame.Clone(),
		withStatDescriptions: gvq.withStatDescriptions.Clone(),
		withMatches:          gvq.withMatches.Clone(),
		// clone intermediate query.
		sql:  gvq.sql.Clone(),
		path: gvq.path,
	}
}

// WithGame tells the query-builder to eager-load the nodes that are connected to
// the "game" edge. The optional arguments are used to configure the query builder of the edge.
func (gvq *GameVersionQuery) WithGame(opts ...func(*GameQuery)) *GameVersionQuery {
	query := (&GameClient{config: gvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	gvq.withGame = query
	return gvq
}

// WithStatDescriptions tells the query-builder to eager-load the nodes that are connected to
// the "stat_descriptions" edge. The optional arguments are used to configure the query builder of the edge.
func (gvq *GameVersionQuery) WithStatDescriptions(opts ...func(*StatDescriptionQuery)) *GameVersionQuery {
	query := (&StatDescriptionClient{config: gvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	gvq.withStatDescriptions = query
	return gvq
}

// WithMatches tells the query-builder to eager-load the nodes that are connected to
// the "matches" edge. The optional arguments are used to configure the query builder of the edge.
func (gvq *GameVersionQuery) WithMatches(opts ...func(*MatchQuery)) *GameVersionQuery {
	query := (&MatchClient{config: gvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	gvq.withMatches = query
	return gvq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		VersionNumber int `json:"version_number,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.GameVersion.Query().
//		GroupBy(gameversion.FieldVersionNumber).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (gvq *GameVersionQuery) GroupBy(field string, fields ...string) *GameVersionGroupBy {
	gvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &GameVersionGroupBy{build: gvq}
	grbuild.flds = &gvq.ctx.Fields
	grbuild.label = gameversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		VersionNumber int `json:"version_number,omitempty"`
//	}
//
//	client.GameVersion.Query().
//		Select(gameversion.FieldVersionNumber).
//		Scan(ctx, &v)
func (gvq *GameVersionQuery) Select(fields ...string) *GameVersionSelect {
	gvq.ctx.Fields = append(gvq.ctx.Fields, fields...)
	sbuild := &GameVersionSelect{GameVersionQuery: gvq}
	sbuild.label = gameversion.Label
	sbuild.flds, sbuild.scan = &gvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a GameVersionSelect configured with the given aggregations.
func (gvq *GameVersionQuery) Aggregate(fns ...AggregateFunc) *GameVersionSelect {
	return gvq.Select().Aggregate(fns...)
}

func (gvq *GameVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range gvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, gvq); err != nil {
				return err
			}
		}
	}
	for _, f := range gvq.ctx.Fields {
		if !gameversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if gvq.path != nil {
		prev, err := gvq.path(ctx)
		if err != nil {
			return err
		}
		gvq.sql = prev
	}
	return nil
}

func (gvq *GameVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*GameVersion, error) {
	var (
		nodes       = []*GameVersion{}
		withFKs     = gvq.withFKs
		_spec       = gvq.querySpec()
		loadedTypes = [3]bool{
			gvq.withGame != nil,
			gvq.withStatDescriptions != nil,
			gvq.withMatches != nil,
		}
	)
	if gvq.withGame != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, gameversion.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*GameVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &GameVersion{config: gvq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(gvq.modifiers) > 0 {
		_spec.Modifiers = gvq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, gvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := gvq.withGame; query != nil {
		if err := gvq.loadGame(ctx, query, nodes, nil,
			func(n *GameVersion, e *Game) { n.Edges.Game = e }); err != nil {
			return nil, err
		}
	}
	if query := gvq.withStatDescriptions; query != nil {
		if err := gvq.loadStatDescriptions(ctx, query, nodes,
			func(n *GameVersion) { n.Edges.StatDescriptions = []*StatDescription{} },
			func(n *GameVersion, e *StatDescription) {
				n.Edges.StatDescriptions = append(n.Edges.StatDescriptions, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := gvq.withMatches; query != nil {
		if err := gvq.loadMatches(ctx, query, nodes,
			func(n *GameVersion) { n.Edges.Matches = []*Match{} },
			func(n *GameVersion, e *Match) { n.Edges.Matches = append(n.Edges.Matches, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range gvq.withNamedStatDescriptions {
		if err := gvq.loadStatDescriptions(ctx, query, nodes,
			func(n *GameVersion) { n.appendNamedStatDescriptions(name) },
			func(n *GameVersion, e *StatDescription) { n.appendNamedStatDescriptions(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range gvq.withNamedMatches {
		if err := gvq.loadMatches(ctx, query, nodes,
			func(n *GameVersion) { n.appendNamedMatches(name) },
			func(n *GameVersion, e *Match) { n.appendNamedMatches(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range gvq.loadTotal {
		if err := gvq.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (gvq *GameVersionQuery) loadGame(ctx context.Context, query *GameQuery, nodes []*GameVersion, init func(*GameVersion), assign func(*GameVersion, *Game)) error {
	ids := make([]guidgql.GUID, 0, len(nodes))
	nodeids := make(map[guidgql.GUID][]*GameVersion)
	for i := range nodes {
		if nodes[i].game_version_game == nil {
			continue
		}
		fk := *nodes[i].game_version_game
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(game.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "game_version_game" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (gvq *GameVersionQuery) loadStatDescriptions(ctx context.Context, query *StatDescriptionQuery, nodes []*GameVersion, init func(*GameVersion), assign func(*GameVersion, *StatDescription)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[guidgql.GUID]*GameVersion)
	nids := make(map[guidgql.GUID]map[*GameVersion]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(gameversion.StatDescriptionsTable)
		s.Join(joinT).On(s.C(statdescription.FieldID), joinT.C(gameversion.StatDescriptionsPrimaryKey[0]))
		s.Where(sql.InValues(joinT.C(gameversion.StatDescriptionsPrimaryKey[1]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(gameversion.StatDescriptionsPrimaryKey[1]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(guidgql.GUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*guidgql.GUID)
				inValue := *values[1].(*guidgql.GUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*GameVersion]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*StatDescription](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "stat_descriptions" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (gvq *GameVersionQuery) loadMatches(ctx context.Context, query *MatchQuery, nodes []*GameVersion, init func(*GameVersion), assign func(*GameVersion, *Match)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[guidgql.GUID]*GameVersion)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Match(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(gameversion.MatchesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.game_version_matches
		if fk == nil {
			return fmt.Errorf(`foreign-key "game_version_matches" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "game_version_matches" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (gvq *GameVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := gvq.querySpec()
	if len(gvq.modifiers) > 0 {
		_spec.Modifiers = gvq.modifiers
	}
	_spec.Node.Columns = gvq.ctx.Fields
	if len(gvq.ctx.Fields) > 0 {
		_spec.Unique = gvq.ctx.Unique != nil && *gvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, gvq.driver, _spec)
}

func (gvq *GameVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(gameversion.Table, gameversion.Columns, sqlgraph.NewFieldSpec(gameversion.FieldID, field.TypeString))
	_spec.From = gvq.sql
	if unique := gvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if gvq.path != nil {
		_spec.Unique = true
	}
	if fields := gvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, gameversion.FieldID)
		for i := range fields {
			if fields[i] != gameversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := gvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := gvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := gvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := gvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (gvq *GameVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(gvq.driver.Dialect())
	t1 := builder.Table(gameversion.Table)
	columns := gvq.ctx.Fields
	if len(columns) == 0 {
		columns = gameversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if gvq.sql != nil {
		selector = gvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if gvq.ctx.Unique != nil && *gvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range gvq.predicates {
		p(selector)
	}
	for _, p := range gvq.order {
		p(selector)
	}
	if offset := gvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := gvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedStatDescriptions tells the query-builder to eager-load the nodes that are connected to the "stat_descriptions"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (gvq *GameVersionQuery) WithNamedStatDescriptions(name string, opts ...func(*StatDescriptionQuery)) *GameVersionQuery {
	query := (&StatDescriptionClient{config: gvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if gvq.withNamedStatDescriptions == nil {
		gvq.withNamedStatDescriptions = make(map[string]*StatDescriptionQuery)
	}
	gvq.withNamedStatDescriptions[name] = query
	return gvq
}

// WithNamedMatches tells the query-builder to eager-load the nodes that are connected to the "matches"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (gvq *GameVersionQuery) WithNamedMatches(name string, opts ...func(*MatchQuery)) *GameVersionQuery {
	query := (&MatchClient{config: gvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if gvq.withNamedMatches == nil {
		gvq.withNamedMatches = make(map[string]*MatchQuery)
	}
	gvq.withNamedMatches[name] = query
	return gvq
}

// GameVersionGroupBy is the group-by builder for GameVersion entities.
type GameVersionGroupBy struct {
	selector
	build *GameVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (gvgb *GameVersionGroupBy) Aggregate(fns ...AggregateFunc) *GameVersionGroupBy {
	gvgb.fns = append(gvgb.fns, fns...)
	return gvgb
}

// Scan applies the selector query and scans the result into the given value.
func (gvgb *GameVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gvgb.build.ctx, "GroupBy")
	if err := gvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GameVersionQuery, *GameVersionGroupBy](ctx, gvgb.build, gvgb, gvgb.build.inters, v)
}

func (gvgb *GameVersionGroupBy) sqlScan(ctx context.Context, root *GameVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(gvgb.fns))
	for _, fn := range gvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*gvgb.flds)+len(gvgb.fns))
		for _, f := range *gvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*gvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// GameVersionSelect is the builder for selecting fields of GameVersion entities.
type GameVersionSelect struct {
	*GameVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (gvs *GameVersionSelect) Aggregate(fns ...AggregateFunc) *GameVersionSelect {
	gvs.fns = append(gvs.fns, fns...)
	return gvs
}

// Scan applies the selector query and scans the result into the given value.
func (gvs *GameVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, gvs.ctx, "Select")
	if err := gvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*GameVersionQuery, *GameVersionSelect](ctx, gvs.GameVersionQuery, gvs, gvs.inters, v)
}

func (gvs *GameVersionSelect) sqlScan(ctx context.Context, root *GameVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(gvs.fns))
	for _, fn := range gvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*gvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := gvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
